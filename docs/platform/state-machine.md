# State Machine Platform

## Purpose
A universal, reusable Finite State Machine (FSM) engine to replace scattered UI-driven status updates across all logistics domains.

## Components
- `StateMachineEngine` (Aggregate)
- `StatusDefinition` (ValueObject)

## Dependencies
None. Fully agnostic.