# Security Overview
Defines the DDD boundaries and Interface-first architecture for the Security Domain.
By defining strict interfaces (`IAuthenticationProvider`, `IAuditService`), the domain is decoupled from infrastructure logic.
