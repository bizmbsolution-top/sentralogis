/**
 * Database type surface for the application.
 *
 * The full `Database` type (v2 supabase-js shape, including table
 * `Relationships` metadata used for typed embedded selects) is generated
 * from the live database by `scripts/generate-db-types.mjs`.
 */
export type { Database, Json } from './database.types';
