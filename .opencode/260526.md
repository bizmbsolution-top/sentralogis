# Rencana Kerja — 26 Mei 2026

## Tema: Perbaiki Driver Portal Update Flow + Lanjut WMS Foundation

### 📋 Status 25 Mei (Eval)
| Item | Status | Keterangan |
|------|--------|-----------|
| `formatDate` error driver portal | ✅ Done | Deployed |
| View Log `/sbu/trucking/tracking` | ✅ Done | Deployed |
| Portal driver update job order | ❌ **Gagal** | Status mapping mismatch: portal pakai `DITERIMA`/`STARTED`/`LOADING`, API simpan `ORDER DITERIMA`/`DALAM PERJALANAN` — button config tidak cocok setelah update |
| WMS Foundation (skema DB + UI) | ❌ Belum dimulai | |
| Testing Trucking Flow | ❌ Belum dimulai | |
| WA Notif ke Driver JOJON | ⏳ Pending | `wa_link_sent_at` masih null |

---

### 🎯 Fokus Utama Besok

#### 🔴 PRIORITAS 1: Fix Driver Portal Update Flow (Pagi)
*Akar masalah: Status mapping mismatch antara button config (`getJobActionButtonConfig`) dan output API*

- [ ] **1a. Audit status flow lengkap** — trace setiap tombol dari klik → API → response → button selanjutnya
  - [ ] Baca `getJobActionButtonConfig` (line ~988) — catat semua status yang dicek
  - [ ] Baca `PATCH /api/jo/[token]` handler — catat semua status yang ditulis API
  - [ ] Baca `handleUpdateJobStatus` — verifikasi mapping yang dikirim frontend
  - [ ] Bikin matrix: status sebelum klik → tombol → API writes status → status setelah reload → tombol selanjutnya muncul?
- [ ] **1b. Fix mismatch** — sync `getJobActionButtonConfig` constants dengan output API
- [ ] **1c. Fix `handleUpdateJobStatus` completion flow** — fleet update & driver_stats increment pakai error handling, jangan silent fail
- [ ] **1d. Fix `updateRouteStatus` sequential validation** — pastikan urutan stop tidak bisa dilompati
- [ ] **1e. Test manual** — login sbg JOJON → accept JO → start journey → update route → complete → verifikasi semua button muncul
- [ ] **1f. Deploy fix** ke Vercel production

#### 🟡 PRIORITAS 2: Kirim WA Notification ke Driver (Pagi-Siang)
- [ ] **2a. Cek endpoint** `api/whatsapp/send-template` — bagaimana cara kerjanya?
- [ ] **2b. Kirim WA** ke 085218129978 untuk JO HALU-TPS-0526-001-01
- [ ] **2c. Update** `wa_link_sent_at` di Supabase setelah sukses
- [ ] **2d. Verifikasi** driver menerima WA

#### 🟢 PRIORITAS 3: Lanjut WMS Foundation (Siang-Sore)
*Lanjut dari 250526.md yang belum tersentuh*

- [ ] **3a. Migration SQL** — tabel `md_warehouses`, `md_warehouse_locations`, `md_product_skus`, `wh_tasks`, `wh_inventory`
- [ ] **3b. RLS policies** — semua tabel WMS berbasis `tenant_id`
- [ ] **3c. CreateWOForm.tsx** — tambah form khusus saat SBU `WAREHOUSE` (INBOUND/OUTBOUND, SKU, qty)
- [ ] **3d. Dashboard SBU Warehouse** — scaffold `app/(dashboard)/sbu/warehouse/work-orders/page.tsx`
- [ ] **3e. Deploy migration** ke Supabase production

#### 🔵 PRIORITAS 4: Testing Trucking Flow (Sore-Malam)
- [ ] **4a. Test case dokumentasi** — `.opencode/testing/trucking-flow.md`
- [ ] **4b. Manual test** — HQ → SBU → Assign → Driver Accept → Start → Route → Complete
- [ ] **4c. Edge case test** — reject, timeout 2 jam, double-assign, WA gagal, token invalid
- [ ] **4d. Fix & deploy** berdasarkan hasil testing

---

### 📁 File yang Akan Dimodifikasi

**Fix Driver Portal:**
- `app/driver/portal/page.tsx` — MODIFY (status mapping, error handling)

**WMS:**
- `supabase/migrations/038_wms_schema.sql` — NEW
- `app/(dashboard)/sbu/warehouse/work-orders/page.tsx` — NEW
- `app/(dashboard)/hq/work-orders/components/CreateWOForm.tsx` — MODIFY
- `app/(dashboard)/sbu/warehouse/layout.tsx` — NEW

**Testing:**
- `.opencode/testing/trucking-flow.md` — NEW

---

### ⚠️ Catatan Penting
- **AGENTS.md wajib dibaca** sebelum mulai coding
- Jangan tebak-nebak tabel Supabase — cek dulu schema via migration files atau tanya
- Semua kredensial dari `.env.local` — jangan hardcode
- Fix driver portal dulu (P1) sebelum apapun — ini blocking driver JOJON
- WA notification pakai endpoint yang sudah ada, jangan buat dari nol
