# Rencana Kerja — 25 Mei 2026

## Tema: Perdalam SBU WMS + Testing

### 🎯 Fokus Utama
1. **SBU Warehouse** — lanjutkan fondasi WMS: halaman manager, inbound/outbound flow, integrasi HQ
2. **Testing** — buat test plan dan eksekusi untuk trucking flow yang sudah berjalan

---

### 📋 Task 1: Lanjut Fondasi WMS (Pagi)

#### 1a. Eksekusi Skema Database WMS
- [ ] Jalankan migration SQL untuk tabel WMS di Supabase:
  - `md_warehouses` — daftar gudang/gedung
  - `md_warehouse_locations` — area, zona, rak/bin
  - `md_product_skus` — spesifikasi komoditas (dimensi, berat, FEFO/FIFO)
  - `wh_tasks` — task receiving/picking (analog `job_orders`)
  - `wh_inventory` — inventory_id dan koordinat stok aktual
- [ ] Set RLS policies berbasis `tenant_id`

#### 1b. Integrasi HQ → WMS
- [ ] Buka `CreateWOForm.tsx` — tambah form khusus saat SBU `WAREHOUSE` dipilih
- [ ] Field: tipe task (**INBOUND** / **OUTBOUND**), daftar SKU, quantity
- [ ] Simpan data spesifik gudang di kolom `item_data` tabel `wo_items`

#### 1c. Dashboard SBU Warehouse Manager
- [ ] Scaffold layout: `app/(dashboard)/sbu/warehouse/work-orders/page.tsx`
- [ ] Query `wo_items` dengan `sbu_type = 'WAREHOUSE'`
- [ ] Tabel interaktif inbound/outbound tasks

---

### 📋 Task 2: Testing Trucking Flow (Siang-Sore)

#### 2a. Test Plan
- [ ] Buat test case untuk flow lengkap:
  - **HQ → SBU Trucking:** WO dibuat → JO muncul di halaman assignments
  - **Assignment:** Pilih fleet + driver → WA terkirim
  - **Driver Response:** Accept/Reject → status berubah
  - **Lifecycle:** Start Journey → Load → Unload → Complete
  - **Keuangan:** Advance dibayar → Pelunasan → Sisa Pelunasan muncul
- [ ] Dokumentasi test di `.opencode/testing/trucking-flow.md`

#### 2b. Manual Testing (via Browser)
- [ ] Login sebagai HQ → buat WO trucking → cek muncul di SBU
- [ ] Login sebagai SBU Trucking → assign fleet/driver
- [ ] Buka driver portal → accept JO → start journey → complete
- [ ] Verifikasi link tracking (`driver_link_token`, `tracking_token`)
- [ ] Verifikasi print slip kasbon
- [ ] Cek finance dashboard (Total Hak, Advance, Pelunasan, Outstanding)

#### 2c. Edge Cases
- [ ] Driver reject JO → muncul rejection note di SBU
- [ ] Driver tidak accept >2 jam → alert di observability
- [ ] Fleet double-assign → deteksi di monitoring
- [ ] Token invalid/tracking page error handling
- [ ] WhatsApp gagal terkirim (nomor invalid)

---

### 📋 Task 3: Bug Fixes from Testing (Sore-Malam)

- [ ] Fix issues berdasarkan hasil testing
- [ ] Deploy fix ke Vercel
- [ ] Verifikasi production

---

### 📁 File yang Akan Dibuat/Dimodifikasi

**WMS:**
- `supabase/migrations/038_wms_schema.sql` — NEW
- `app/(dashboard)/sbu/warehouse/work-orders/page.tsx` — NEW
- `app/(dashboard)/hq/work-orders/components/CreateWOForm.tsx` — MODIFY
- `app/(dashboard)/sbu/warehouse/layout.tsx` — NEW (sidebar WMS)

**Testing:**
- `.opencode/testing/trucking-flow.md` — NEW

---

### ⚠️ Catatan Penting
- **Database First:** Semua UI WMS harus menunggu migration SQL dijalankan
- **Ikuti pola Trucking:** Arsitektur WMS mirip Trucking — WO dari HQ → Task di SBU
- **AGENTS.md wajib dibaca** sebelum mulai coding
- Tidak ada `entity_id` di profile — entity scope via transporter/sbu context
